# Core dependencies
pxr>=2.0.0  # Pixar USD library
flask>=2.0.0  # For HTTP API
requests>=2.25.0  # For client HTTP requests
typing-extensions>=4.0.0  # For type hints
numpy>=1.19.0  # For numerical operations
pillow>=8.0.0  # For image processing (if needed)
colorama>=0.4.4  # For colored terminal output
pyzmq>=22.0.0  # For ZeroMQ protocol support (optional)
websockets>=10.0  # For WebSocket support (optional)

# AI integration dependencies
openai>=1.0.0  # For ChatGPT integration
anthropic>=0.5.0  # For Claude integration

# Visualization dependencies
jinja2>=3.0.0  # For HTML template rendering
pygments>=2.10.0  # For syntax highlighting

# Omniverse specific (comment out if not available)
# omni-kit-py>=104.0.0  # Omniverse Kit Python bindings
# carbonite>=104.0.0  # Omniverse performance optimization

# Development and testing
pytest>=6.0.0  # For testing
flake8>=4.0.0  # For linting
black>=22.0.0  # For code formatting

# Add any other dependencies your project needs 